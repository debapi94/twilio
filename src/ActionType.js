

const ACTION_TYPE = {
    START_VIDEO:'start video',
    CLOSE_VIDEO:'close video',
    VIDEO_LOADED:'video loaded',
    JOIN_CALL:'join call',
    REMOTE_PARTICIPANT_CONNECTED:'remote participant connected',
    ADD_REMOTE_PARTICIPANT_TRACKS:'add remote participant track',
    REMOVE_REMOTE_PARTICIPANT_TRACKS:'remove remote participant track'
  }

  export default ACTION_TYPE;